{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Oracle Bootstrap (Generate Corpus + Parity)",
      "type": "shell",
      "command": "npm run test:oracle-bootstrap",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    },
    {
      "label": "Oracle Parity (Openplanet)",
      "type": "shell",
      "command": "npm run test:oracle-parity",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "group": {
        "kind": "test",
        "isDefault": true
      }
    },
    {
      "label": "Oracle Parity Matrix (All Versions)",
      "type": "shell",
      "command": "npm run test:oracle-parity:matrix -- --strict",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
